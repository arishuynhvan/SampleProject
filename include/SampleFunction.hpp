#ifndef SENSITIVITYANALYSIS_INCLUDE_FILENAME_HPP_
#define SENSITIVITYANALYSIS_INCLUDE_FILENAME_HPP_

// System includes first, e.g. #include <cmath>
// User includes next, e.g. #include "DataFileReader.hpp"
// Note: your file name should match the function/class name

namespace SensitivityAnalysis
{
/**
 * Function/Class description in doxygen format. Remember to document your parameters
 * and return value(s) if any. 
 *
 * \param x Dummy argument
 * \return an error code, 0 means success, 1 means failure
 */
int SampleFunction(int x);

}  // namespace SensitivityAnalysis

#endif
